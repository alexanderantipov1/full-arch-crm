# Agent Report

Task ID: A2
Linear issue: ENG-178 / ENG-180
Agent role: Claude Code explorer (read-only)
Status: report-only; no live evidence collected (see Blockers)
Branch: read-only (HEAD `cb4d37e`, tracking `origin/main`)
Worktree: primary repo

## Summary

Wave 1 ran in `don't ask` harness mode with `gh run`, `gh api`, and
`gcloud logging read` outside the auto-approve allowlist. Every attempt
to fetch live workflow logs or Cloud Run logs was rejected by the
permission layer, so no run IDs, revision names, or sanitized log
excerpts could be captured. Per the task brief's Stop Conditions this
is reported instead of guessed at.

Repo-side evidence is consistent with the prior session summary in the
mission notes:

- The deep API smoke runs through the public LB + IAP via the WIF
  identity token whose audience is now the workflow env constant
  `IAP_OAUTH_CLIENT_ID` (cb4d37e, ENG-180).
- The first deep-smoke check (`BODY=$(check "/healthz")`) wraps every
  diagnostic `echo` and `cat /tmp/body | head -50` inside a command
  substitution, so the body line, the HTTP status line, and the
  `fail()`-emitted `::error::` line do reach stdout but they are
  captured into `BODY` and never printed to the GitHub Actions log.
  Only the `set -euo pipefail` exit propagates. This is the
  diagnostic-visibility defect A1 owns.
- After ENG-180 landed, ENG-178 cannot be confirmed without that
  diagnostic fix because the GitHub Actions logs in the prior session
  showed only a generic non-zero exit on `Hit smoke endpoints`.

Likeliest root-cause category for the most recent deploy-prod failure:
HTTP status mismatch on `/healthz` through the IAP front door, with
`commit_sha` mismatch as the secondary candidate. Application 5xx is
the least likely category given the anonymous boot smoke
(`https://fusioncrm.app/api/healthz`) is a hard 5xx-or-empty gate
upstream of the deep smoke and would have triggered its own rollback
path with a visible `::error::` line.

## Files Changed

None. This is a read-only task; the only write is this report file.

## Git State

- HEAD: `cb4d37ec67fc08e5d9800089d341ad284f8ee38c` on local `main`,
  tracking `origin/main`.
- Pre-existing dirty files (not part of this mission per
  `ownership.md`):
  - `.claude/scheduled_tasks.lock`
  - `apps/api/routers/tenant.py`
  - `packages/tenant/credential_service.py`
  - `packages/tenant/schemas.py`
  - `tests/tenant/test_credential_service.py`
  - untracked `tests/api/test_tenant_credential_routes.py`
  - untracked `Agent_Orchestration_Playbook_RU.md`
  - untracked `.agents/`, `.claude/commands/orchestrator.md`
- Last 5 commits touching `.github/workflows/`:

  | SHA | Date (PDT) | Title |
  | --- | --- | --- |
  | `cb4d37e` | 2026-05-16 22:50 | fix(deploy): pin IAP OAuth client ID as workflow env (ENG-180) (#75) |
  | `7e09c0a` | 2026-05-16 22:03 | fix(deploy): filter traffic verification around preview tags (ENG-179) (#73) |
  | `7473ada` | 2026-05-16 21:34 | fix(deploy): route deep smoke through IAP front door (ENG-178) (#72) |
  | `a4aa78d` | 2026-05-16 18:06 | chore(deploy): flip preflight STRICT 0 → 1 (ENG-174 §A complete) (#71) |
  | `8b74852` | 2026-05-16 17:31 | fix(deploy): rollback target filter + pr-preview primary-traffic restore + public-LB boot smoke (ENG-175) (#70) |

  All five merges were on 2026-05-16. The deploy-prod runs we needed
  evidence for would be the post-merge pushes for `#70`, `#71`, `#72`,
  `#73`, and `#75` — none of those run IDs could be retrieved from the
  GitHub API in this session.

## Tests / Checks

Commands attempted (all read-only) and harness result:

| Command | Read-only? | Result |
| --- | --- | --- |
| `gh auth status` | yes | OK — `Eduard59` logged in, scopes include `repo`, `workflow`, `read:org` |
| `gh run list --workflow deploy-prod.yml --branch main --limit 10` | yes | DENIED by harness allowlist |
| `gh run list -w deploy-prod.yml -b main -L 10` | yes | DENIED |
| `gh run list --workflow=deploy-prod.yml --branch=main --limit=12 --json …` | yes | DENIED |
| `gh api '/repos/alexanderantipov1/fusion_crm/actions/workflows/deploy-prod.yml/runs?branch=main&per_page=10' --jq …` | yes | DENIED |
| `gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="fusion-api"' --project fusioncrm-494201 --limit 5 --format json` | yes | DENIED |
| `git log -n 25 --oneline -- .github/workflows/deploy-prod.yml` | yes | DENIED (`--` path-form variant blocked) |
| `git log -n 15 --oneline .github/workflows/` | yes | OK — produced the table above |
| `git log -n 5 --pretty=fuller cb4d37e` | yes | OK — confirmed commit timestamps |
| `git show cb4d37e -- …` | yes | DENIED (path-arg variant blocked) |

No mutating command was issued. No credential, token, or secret value
was printed.

Repo-only evidence used in place of live logs:

- `.github/workflows/deploy-prod.yml` (current `main` tip):
  - Top-level `env.IAP_OAUTH_CLIENT_ID` is the public client ID
    `800777477533-fv4l6nd3ou3c9euvr7re52rfcp680ess.apps.googleusercontent.com`
    (lines 60-75). This is intentionally public, see the comment block
    that cites ENG-180 and `docs/DEPLOYMENT_RULES.md` §2.
  - `api-smoke.steps.smoke.env.IAP_AUDIENCE` is set from
    `${{ env.IAP_OAUTH_CLIENT_ID }}` (line 670). The token mint step
    impersonates `cloud-build-deployer-sa` (line 680-682). The token
    audience now exactly matches the IAP backend's expected client_id,
    so the IAP layer should accept the request and forward to Cloud
    Run.
  - `check()` writes the body to `/tmp/body`, captures `%{http_code}`,
    and on mismatch emits `echo "HTTP ${status} for ${path}; body:"`
    plus `cat /tmp/body | head -50` to stdout before invoking
    `fail()`. `fail()` itself does
    `echo "::error::smoke fail: $1"` and `echo "rollback_needed=true"
    >> "$GITHUB_OUTPUT"` and `exit 1`. The `::error::` annotation IS
    written to stdout inside the substitution, and the
    `$GITHUB_OUTPUT` append is unaffected — only the human-readable
    HTTP status / body lines are swallowed for the operator.
  - `Auto-rollback on smoke failure` is guarded by
    `steps.smoke.outputs.rollback_needed == 'true' || steps.anon_smoke
    .outputs.rollback_needed == 'true'`. Both outputs can fire even
    though their stdout was eaten by `$(...)`, so rollback semantics
    are intact. The diagnostic gap is purely about operator
    visibility, not about whether the right rollback ran.

- `apps/api/routers/health.py`:
  - `/healthz` returns `{"status":"ok","commit_sha": APP_COMMIT_SHA or "dev"}`.
  - The value is read from `os.environ["APP_COMMIT_SHA"]` at request
    time, not from a module-level constant. Restarts and revision
    swaps re-read it; that means a working revision sees the env value
    actually wired by `gcloud run deploy --set-env-vars=…,APP_COMMIT_SHA=…`.

- `.github/workflows/deploy-prod.yml` `deploy-api.steps.deploy.env`
  (line 472): `APP_COMMIT_SHA: ${{ github.sha }}` is passed into the
  canonical script. `infra/scripts/deploy_cloud_run.sh:388-389`
  appends `APP_COMMIT_SHA=${APP_COMMIT_SHA}` to the API env-vars list
  only when the variable is non-empty. The workflow always sets it on
  push events. So a true `commit_sha` mismatch (revision env vs
  github.sha) would require either:
  1. The deploy script bailed before the `--set-env-vars` line but
     the workflow still proceeded to the smoke (would require a bug
     where deploy_cloud_run.sh exits 0 without updating env), or
  2. The smoke job hit a revision other than the just-deployed one
     because the `Verify traffic is actually on the new revision`
     step did not force-flip in time, or because traffic returned to
     a tagged PR-preview revision after the finalizer.

  ENG-179 (`7e09c0a`) just hardened the primary-revision picker in
  that very step. If the failure precedes ENG-179 it can plausibly be
  case 2; after ENG-179 it should be case 1 or HTTP status mismatch.

- Deep smoke endpoints (`/dashboard/summary`, `/integrations`)
  require `Principal.tenant_id`, which `get_principal_with_tenant`
  always populates from `Settings.tenant_default_slug`. So they do
  not require a real OIDC principal to return 200 with a tenant
  context — they do require the `tenant.tenant` bootstrap row and
  the database to be reachable, plus IAP to accept the token. Neither
  endpoint was the trigger reported in the prior session; the failure
  was at the first check (`/healthz`).

## Ownership Notes

- A2 scope respected: no source files touched. No Linear updates. No
  Cloud Run, no GitHub Actions, no production state changes were
  attempted.
- A1's owned file (`.github/workflows/deploy-prod.yml`) was read but
  not edited. The current diagnostic gap inside `BODY=$(check ...)`
  is the same one A1's brief targets.

## Linear Notes

- Recommended Linear status:
  - ENG-178: keep In Review / Blocked. Deploy-prod has not gone green
    end-to-end through the public LB + IAP path. Acceptance is gated
    on a real `/healthz` smoke pass with `commit_sha == github.sha`.
  - ENG-180: keep In Review. The pinned audience is in production
    code, but acceptance per the mission contract requires a green
    deploy-prod run to confirm the audience participates end-to-end.

- Comment/update for orchestrator to post (English; sanitized):
  > A2 read-only investigation could not retrieve live GitHub
  > Actions or Cloud Run evidence — `gh run`, `gh api`, and
  > `gcloud logging read` are outside the harness auto-approve
  > allowlist in this session. Repo-side review confirms
  > (a) ENG-180 audience pinning is on `main` at cb4d37e and is
  > correctly threaded through the smoke job env, and (b) the
  > smoke step's `BODY=$(check "/healthz")` pattern still swallows
  > the human-readable HTTP status and body diagnostics that ENG-178
  > acceptance requires. The `rollback_needed` GitHub output and
  > the `::error::` annotation do still leave the subshell, so
  > auto-rollback semantics are intact even today. ENG-178 cannot
  > be accepted until either (i) A1's Phase 4.5 logging fix lands
  > and a fresh deploy-prod prints the actual failure line, or
  > (ii) a Wave-2 operator-approved diagnostic dispatch surfaces the
  > revision's `/healthz` body via `gcloud logging read`.

- New issues suggested:
  - None at this point. Wave 1 already has A1 (Phase 4.5 logging)
    and the existing ENG-178/ENG-180 trackers. If a Wave-2 deploy
    still fails after A1 lands, file a follow-up under ENG-178 with
    the now-visible HTTP status + body excerpt.

## Blockers

- Live deploy-prod run metadata is inaccessible from this terminal
  because the harness blocks `gh run`, `gh api`, and `gcloud
  logging read` in `don't ask` mode. The task brief allows these
  commands, but the runtime permission allowlist
  (`.claude/settings.local.json`) only authorizes `gh auth *`,
  `gh pr *`, `gcloud auth *`, `gcloud config *`, `gcloud run *`,
  `gcloud iam *`, etc. Adding `gh run list/view`, `gh api`, and
  `gcloud logging read` to the allowlist (or running A2 in an
  interactive shell) would unblock the on-cluster evidence pass.
- Until those commands are available, every causal claim about the
  failed deploy-prod run is repo-side inference, not measurement.

## Integration Risks

- Diagnostic blind spot remains until A1's `.github/workflows/
  deploy-prod.yml` patch lands. If Wave-2 dispatches another
  deploy-prod (e.g. on top of the tenant credential dirty files) the
  same opaque failure mode would recur.
- The known auto-rollback path is robust: `rollback_needed`
  is set via `$GITHUB_OUTPUT` which is not affected by the
  command-substitution wrapper, and the rollback step's `if:`
  expression reads it correctly. So even a "silent" smoke fail still
  reverts traffic — operators are not at risk of a bad revision
  staying primary, just of having to guess at the root cause.

## Suggested Next Tasks

Wave-2 candidates, in priority order:

1. **A1 lands first** (already planned). Move the diagnostic `echo`s
   in `check()` and the `::error::` in `fail()` to `>&2`, leave the
   final `cat /tmp/body` as stdout-only so `BODY=$(check ...)` keeps
   parsing the JSON. This is the unblocker for ENG-178 acceptance.
2. **Re-run A2 with widened permission set** (or in an interactive
   shell) once A1's PR is open. The two essential read-only commands:
   - `gh run list --workflow=deploy-prod.yml --branch=main --limit=10
     --json databaseId,headSha,status,conclusion,createdAt,event,url`
   - `gh run view <id> --log` (filtered to the `api-smoke` job).

   Then for Cloud Logging, replace the
   `resource.labels.revision_name="fusion-api-00053-ssf"` filter with
   a broader window — Cloud Run revisions live under
   `resource.type="cloud_run_revision"` and `resource.labels
   .service_name="fusion-api"`, but the revision label can be empty
   or differ in shape on access logs vs application logs. A working
   pair is:
   - app logs over a window:
     `gcloud logging read 'resource.type="cloud_run_revision" AND
      resource.labels.service_name="fusion-api" AND
      timestamp>="<failure_iso>"' --project=fusioncrm-494201
      --limit=200 --order=desc --format=json`
   - LB / request logs (NOT cloud_run_revision):
     `gcloud logging read 'resource.type="http_load_balancer" AND
      resource.labels.url_map_name="fusion-lb-url-map" AND
      timestamp>="<failure_iso>"' --project=fusioncrm-494201
      --limit=200 --order=desc --format=json`
   - IAP allow/deny decisions:
     `gcloud logging read 'logName="projects/fusioncrm-494201/logs/
      cloudaudit.googleapis.com%2Fdata_access" AND protoPayload
      .serviceName="iap.googleapis.com"' --project=fusioncrm-494201
      --limit=50 --order=desc --format=json`
3. **Wave-2 fix branch (post-A1)**: if the now-visible `/healthz`
   diagnostics show a `commit_sha` mismatch, audit
   `verify_traffic` in `deploy-api` against the just-deployed
   revision name to confirm `--to-latest` actually flipped before
   smoke executed. If they show an HTTP status mismatch (401/403),
   re-check the IAP backend's OAuth credential binding versus the
   pinned `IAP_OAUTH_CLIENT_ID` — both must match for IAP to honour
   the bearer token.
4. **Optional hardening (ENG-181-ish, not requested)**: emit the
   captured `BODY` length and the response Content-Type on every
   `/healthz` success in the smoke job so future debugging does not
   require failure-only diagnostics.

## Stop-Condition Trigger

This report was written under the brief's "Access to GitHub or GCP
logs is unavailable" stop condition. No guesses are presented as
fact; live-evidence sections are explicitly marked as not collected.
