# R3 — Wave R Verification Scout

## Status

Complete (R1 verification pending — R1 report not yet present).

## Role

Claude Code read-only verifier / reviewer. Owned write scope is this
report only (`reports/R3.md`). No product or migration files were
edited. No destructive git commands were run. No migration
upgrade/downgrade was run.

## R1 Report Availability

- `reports/R1.md` — NOT PRESENT at the time R3 finished.
- `reports/R2.md` — NOT PRESENT at the time R3 finished.

Findings below are based on `tasks/R1.md`, `tasks/R2.md`,
`reports/Q1.md`, `reports/P2-data-foundation-implementation-plan.md`,
`ownership.md`, `contract.md`, current product source, and the
Alembic revision files. Codex must re-run the R1 verification block
once `reports/R1.md` lands.

## Alembic / Migration-Chain Observations

### Current Head

The Alembic revision graph parsed from
`packages/db/alembic/versions/**` is single-headed.

Linear chain (oldest → newest):

```
af6c4e767923 (initial)
b4f8c9a2d1e0 (v0_2 actor_auth_integrations)
a3b1c5d7e9f0 (d2 identity_source_link_merge_event)
390b53c6f4a9 (d1 interaction_event_slim)
4ba791c47185 (d3 ops_account)
c1f9d3a4b8e2 (tenant_domain_create)
d2e0f4b5c9a3 (tenant_id_columns_nullable)
e3a1b6c7d4f5 (tenant_bootstrap_seed)
f4b2c8d9e6a7 (tenant_id_not_null_fk)
b7c3e9f1a2d4 (tenant_integration_credential_multi_mailbox)
a8c5e7d2f4b9 (tenant_credentials_seed)
e8d3a5b1c2f4 (tenant_id_drop_server_default)   ┐
c9d2e4f6a8b3 (outreach_domain_create)          ├ branch
d7e9f5b3c1a8 (outreach_send_campaign_nullable) ┘
af5ba42a505b (merge_parallel_heads_2026_05_10_tenant_) ← merge of e8d3a5b1c2f4 + d7e9f5b3c1a8
b4c2e1f9a5d7 (location_uniqueness_by_carestack_id)
e1f2a3b4c5d6 (add_identity_match_candidate — Q1 / ENG-182)   ← CURRENT HEAD
```

I was unable to confirm with the Alembic CLI (`alembic heads`,
`alembic current`, `alembic check`) — the harness denied all
`alembic`/`cd packages/db && alembic …` invocations in this scout
session under `dontAsk` mode. The chain above was reconstructed by
direct file inspection of every `revision` / `down_revision` field
under `packages/db/alembic/versions/`. Codex must re-confirm with
`alembic heads` and `alembic check` after R1.

### Expected R1 Parent

R1's new revision must set:

```python
down_revision: str | None = "e1f2a3b4c5d6"
```

Any other parent (e.g. `b4c2e1f9a5d7`) would create a parallel head
and require a merge migration. The mission goal explicitly forbids a
second migration writer in Wave R (`contract.md` §"Wave R Contract"),
so the linear-chain assumption must hold.

### R1 Must NOT Touch

- `packages/db/alembic/versions/20260518_2010_e1f2a3b4c5d6_add_identity_match_candidate.py`
  (Q1; shipped). The follow-up FK
  `identity.match_candidate.hint_id` →
  `ingest.normalized_person_hint.id` that Q1 deviation #1 deferred
  must be a NEW additive revision after R1 lands, not an edit of
  `e1f2a3b4c5d6`. R1's brief does not assign that FK — it is a
  follow-up beyond R1.
- Any earlier shipped revision file.

### Pre-existing ENG-188 Model Reconciliation (NOT Drift)

`git status` shows `M packages/ingest/models.py`. The only diff is
the addition of:

```python
Index("ix_raw_event_tenant_id", "tenant_id"),
```

This is the ENG-188 (`Q0`) reconciliation: the index already exists
in Postgres (created by an ENG-123 tenant migration) but had been
missing from ORM metadata. `Q0-eng188-alembic-drift.md` documents
this and verified `alembic check → No new upgrade operations
detected`. R1 must:

- KEEP this line as-is; do not "fix" it by removing the index from
  the model or by adding a fresh migration for it. `alembic check`
  will stay clean.
- Treat the modified-but-uncommitted state of
  `packages/ingest/models.py`, `packages/identity/models.py`, etc.
  as the expected baseline.

### R1 New-Revision Shape Expectations

Cross-checked against `tasks/R1.md` and P2 §"ingest.normalized_person_hint":

- Schema: `ingest`, table `normalized_person_hint`.
- One `op.create_table(...)` followed by `op.create_index(...)`
  calls. Use `op.f(...)` for every constraint name so the project's
  `NAMING_CONVENTION` from `packages/db/base.py` is respected (same
  pattern as Q1's revision).
- FK `raw_event_id → ingest.raw_event.id` (intra-schema; allowed).
- `person_uid` UUID, NO FK (identity rule: ingest may import core
  + tenant + identity at service layer but the column must stay a
  plain UUID per `packages/CLAUDE.md` cross-package matrix and the
  brief's "no identity model/repository import" line).
- `source_link_id` UUID, NO FK (same reason).
- Unique: `(tenant_id, raw_event_id)`.
- Indexes per the brief's bullet list.
- No data backfill in this revision.
- `downgrade()` must drop the indexes then the table (mirror Q1's
  pattern).

### `alembic check` Risk After R1

- If R1 follows the model + migration shape in the brief, autogenerate
  should report "No new upgrade operations detected" after R1
  applies — provided the model includes every index/uniq/CHECK that
  is emitted in the new revision and uses the project mixins
  (`UUIDPrimaryKeyMixin`, `TimestampMixin`, `TenantScopedMixin`).
- Common ways R1 could break `alembic check`:
  - Forgetting to add `Index(...)` lines in `__table_args__` for
    indexes created in the migration.
  - Mixing `op.f(...)` vs literal names so the model's
    `Index("ix_...")` differs from the migration's index name.
  - Forgetting `schema="ingest"` on either the model or the
    migration.
  - Adding a server default on `quality_flags` / `meta` in the
    migration without `server_default=text("'{}'::jsonb")` in the
    model (Q1 hit and avoided this).

## Ownership / Contract Observations

### Write Scopes Are Disjoint (Confirmed)

| Worker | Owned writes (this wave) |
|---|---|
| R1 (writer) | `packages/ingest/{models,schemas,repository,service}.py`, `tests/ingest/`, ONE new `packages/db/alembic/versions/*.py`, `reports/R1.md` |
| R2 (read-only planner) | `reports/R2.md` |
| R3 (this report) | `reports/R3.md` |

- No overlap with Q1's already-committed write scope
  (`packages/identity/**`, `tests/identity/**`, Q1 migration file).
- No overlap with any other open task in `ownership.md`.
- The only coupling between R1 and Q1 is the Alembic chain
  dependency (R1 must parent off `e1f2a3b4c5d6`) and the future
  `hint_id` FK promotion (out of R1 scope; tracked as Q1
  follow-up).

### Contract Compliance

- `contract.md` §"Wave R Contract" allows exactly ONE Wave R
  migration writer (R1). R3 confirms no other worker in this wave
  is authorized to add a migration.
- R1 must not change Salesforce or CareStack ingest behavior. The
  brief reinforces this. ENG-185 (R2 plan) is the legitimate place
  for moving `SfLeadIngestService._resolve_person` matching into
  `IdentityService.resolve_or_create_from_hint(...)`.
- `person_uid` / `source_link_id` as bare UUIDs (no Python import)
  matches the `packages/CLAUDE.md` cross-package rule (`ingest →
  identity = ✗` for models/repos; only services may cross, and the
  hint capture service must not do so).
- PHI/raw-payload exclusion from `meta` / `quality_flags` is
  required by both the brief and the PHI guard pattern Q1
  established (`_FORBIDDEN_EVIDENCE_KEYS` in
  `packages/identity/service.py`). R1 should mirror that recursive
  guard in `IngestService.capture_normalized_person_hint(...)` so
  the same deny-list applies at the ingest boundary.

### Risks Codex Should Flag Pre-Review

1. **`alembic check` regression risk** if R1 ships the migration but
   forgets to add the corresponding model indexes/CHECKs (mirror Q1
   deviations #1-#5 to keep the patterns aligned).
2. **`source_system` / `source_kind` enum validation** — P2 suggests
   either DB CHECK or service validation. The brief leaves this to
   the worker. If R1 picks DB CHECK, the values must match what
   `identity.source_link` uses today so a future
   `IdentityService.resolve_or_create_from_hint(...)` does not
   surface a value mismatch. If R1 picks service validation, the
   allowed list should be documented in the report.
3. **`payload_sha256` reuse vs new column** — the P2 plan notes the
   same field could optionally be added to `ingest.raw_event`. The
   brief deliberately scopes R1 to the new hint table only;
   `raw_event` must stay unchanged this wave. Codex should reject
   any drive-by edit to `ingest.raw_event`.
4. **Test coverage parity with Q1** — Q1 shipped both a model
   metadata test and a service-layer test. R1 should match that
   shape: at least one `tests/ingest/test_normalized_person_hint_model.py`
   for columns/indexes/uniques, and one
   `tests/ingest/test_normalized_person_hint_service.py` for the
   capture service path (PHI guard + idempotency on
   `(tenant_id, raw_event_id)` + `quality_flags` defaulting).
5. **Tenant-scoped uniqueness behaviour** — `(tenant_id, raw_event_id)`
   must hold even though `raw_event_id` itself is already unique by
   PK in `ingest.raw_event`. Including `tenant_id` matches the
   project's per-tenant uniqueness invariant and matches what Q1
   did for partial-unique guards.
6. **R2 dependency on R1** — R2 is read-only and the task brief
   accepts the dependency. R2's plan should explicitly call out
   that the `IdentityService.resolve_or_create_from_hint(...)`
   entry point needs both the `MatchCandidate` ledger (already
   present after Q1) and the `NormalizedPersonHint` row (only
   after R1). Codex should not approve R2 if it implicitly assumes
   R1 has shipped a different shape than the brief defines.

## Recommended Codex Review Checklist For R1

Run after `reports/R1.md` is present and before any Linear status is
moved beyond In Progress:

### A. File-Diff Boundary Checks (Read-Only)

- `git status --short` — confirm R1 only modified files in its
  owned write scope (and the existing Q0 dirty model files Q1
  already inherited).
- `git diff --check` — exits 0 (whitespace-clean).
- `git diff --name-only HEAD` filtered against `ownership.md` R1
  row — every changed product file falls under R1.
- No edits to any file with revision id other than R1's new
  revision in `packages/db/alembic/versions/`.

### B. Migration-Shape Checks

- New revision file's `revision = "<R1 id>"`, `down_revision =
  "e1f2a3b4c5d6"`.
- Exactly one new revision file added under
  `packages/db/alembic/versions/`.
- `upgrade()` creates the `normalized_person_hint` table in schema
  `ingest`, with the columns listed in the brief.
- All CHECK / FK / index names use `op.f(...)` so the project naming
  convention applies (mirrors Q1).
- `downgrade()` drops indexes then table.
- No edit to any other migration file.

### C. ORM / Schema Parity

- `packages/ingest/models.py` defines `NormalizedPersonHint(...,
  TenantScopedMixin, UUIDPrimaryKeyMixin, TimestampMixin, Base)`
  with `__tablename__ = "normalized_person_hint"` and
  `__table_args__` schema `"ingest"`.
- Every index created in the migration has a matching `Index(...)`
  in `__table_args__` (same name) so `alembic check` is clean.
- `person_uid` and `source_link_id` are `Mapped[UUID | None]`
  without `ForeignKey(...)` (plain pointers).
- `raw_event_id` IS a `ForeignKey("ingest.raw_event.id", ...)`
  (intra-schema, allowed).
- `quality_flags`, `meta` Mapped[dict] JSONB with
  `server_default=text("'{}'::jsonb")` and `nullable=False`.

### D. Service / Repository Hygiene

- `IngestRepository.add_normalized_person_hint(...)` uses
  `for_tenant(...)` for any read paths; no cross-tenant fall-through.
- `IngestService.capture_normalized_person_hint(...)` is the only
  public surface; it rejects (recursively) PHI/raw-payload keys in
  `meta` / `quality_flags` (mirror Q1's recursive deny-list).
- No import of `packages.identity.*` repository/model from
  `packages/ingest/*`. Cross-package matrix says `ingest → identity
  = ✗`.
- `packages.audit` writes optional for this slice; not required by
  the brief but Codex should confirm consistency with other
  capture-style services.

### E. Focused Verification (Codex To Run)

- `.venv/bin/python -m pytest tests/ingest -q`
- `set -a; source ./.env; set +a; cd packages/db &&
  ../../.venv/bin/alembic upgrade head`
  (then `downgrade -1` then `upgrade head` for round-trip).
- `set -a; source ./.env; set +a; cd packages/db &&
  ../../.venv/bin/alembic check` → expect `No new upgrade
  operations detected.`.
- `git diff --check`.
- `make verify` (ruff + mypy + deploy-critical pytest) if time
  permits.
- Identity regression check: `.venv/bin/python -m pytest
  tests/identity -q` — Q1 left this at 45 passed. R1 should not
  change this.

### F. Mission-Goal Evidence

- `reports/R1.md` includes: files changed, revision id / down
  revision, P2 deviations, tests/checks, blockers.
- `alembic check` evidence is quoted verbatim.
- No `.env`, secret, deploy script, GitHub Actions workflow, or
  Cloud Run change.
- No commit, push, merge, rebase, or destructive git command.

## Blockers

- None for R3 itself. The R3 owned write scope was only this report.
- Soft blocker for Wave R as a whole: `reports/R1.md` and
  `reports/R2.md` were not present when R3 finished, so the final
  Wave R evaluation cannot be made yet. Codex should re-evaluate
  the mission goal after R1 and R2 reports arrive, using the
  checklist above.
- Migration / contract reminder: any future migration that promotes
  `identity.match_candidate.hint_id` to a real FK against
  `ingest.normalized_person_hint.id` MUST be a new additive
  revision — NOT an edit of `e1f2a3b4c5d6`. Out of scope for R1.

## Files Changed

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R3.md`

## Tests / Checks Run

- None mutating. Attempted `alembic heads` / `alembic current` /
  `alembic check` were denied by the harness in `dontAsk` mode;
  Alembic chain was reconstructed by reading the revision files
  directly. Codex must run the Alembic commands in the post-R1
  review block.

## Linear Notes (For Codex Sync, Not For R3 To Mutate)

- ENG-183 (R1) — leave In Progress until R1 report + Codex review.
- ENG-185 (R2) — leave In Progress until R2 report + Codex review.
- ENG-181 (P2) — stays In Review; this wave is the next slice in
  the same data-foundation plan.
