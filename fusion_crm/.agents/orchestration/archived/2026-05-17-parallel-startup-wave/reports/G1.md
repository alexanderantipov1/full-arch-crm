# Terminal Agent Report

Task ID: G1
Linear issue: ENG-169 (runtime ADR gate) + ENG-168 (Twilio/SMS foundations) — planning only
Agent role: Claude Code explorer / planner
Status: complete
Branch: `main` (primary worktree, no checkout)
Worktree: primary repo

## Summary

Read-only brief for the next implementation wave that follows the current
deploy/tenant bundle (A2-live, B1, D1, E1, F1). The wave has two tracks:
ENG-169 (live-subscriber runtime ADR) and ENG-168 (Twilio + SMS controlled
send). ENG-169 is doc-only and unblocks live ingestion for ENG-166/ENG-167
later; ENG-168 is the first real implementation track because Twilio is the
upstream of every SMS-triggering feature and its outbound-HTTPS shape does
NOT depend on the ENG-169 runtime decision. The two safest first slices are
therefore:

1. ENG-169 → an ADR file at `docs/decisions/ADR-XXXX-live-subscriber-runtime.md`.
2. ENG-168 → a Twilio-credential payload ADR + the `packages/integrations/twilio/`
   read-only client, bundled per `feedback_pr_granularity` (solo dev: bundle
   related slices). Wave kept doc-only until ADR is accepted.

No product file was modified by this task. The only write was this report.

## Files Changed

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/G1.md` — this report.

## Git State

- Current branch: `main`, working tree (no checkout).
- Dirty files: pre-existing mission dirty set (deploy-prod workflow, tenant
  credential service/schema/routes/tests, plus orchestration files under
  `.agents/`). G1 did not touch any of them.
- Commits made: none.
- Push status: none.

## Tests / Checks

- None. G1 is read-only planning; no test runs were performed.

## Ownership Notes

- Writes stayed inside the assigned scope (`reports/G1.md` only).
- No product file, migration, env file, workflow file, deploy script, Linear
  ticket, Cloud Run / Secret Manager / IAP surface was touched.
- Followed `contract.md` (Wave 1 carve-out: no API / schema / UI / migration
  changes assigned in this task) and `mission.md` (no commits, pushes,
  deploys, workflow reruns, Linear mutations).
- No contract changes requested.

## Linear Notes

- Recommended Linear status:
  - ENG-169: keep `Backlog` until the orchestrator approves this brief; then
    move to `Ready` for a single Claude Code explorer who will draft the ADR.
  - ENG-168: keep `Backlog` (still gated by ENG-165 / B1 close + ENG-169 +
    ENG-168 credential ADR). Do NOT move to `Ready` before all three gates
    clear.
- Comment for orchestrator to post (after Codex approves this brief):
  > G1 brief landed in `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/G1.md`.
  > Next implementation wave is ENG-169 (runtime ADR — doc only) followed by
  > ENG-168 split into ENG-168a (Twilio credential payload ADR), ENG-168b
  > (`packages/integrations/twilio/` client), and ENG-168c (`packages/sms/`
  > controlled-send service). Hard prerequisites: ENG-165 / B1 close, ENG-169
  > ADR accepted, ENG-168a ADR accepted.
- New issues suggested (do NOT create from this task — orchestrator + Codex
  approve, then someone with Linear write-scope creates):
  - `ENG-168a` — "Twilio credential payload + IntegrationCredentialService
    contract (ADR)" — design only.
  - `ENG-168b` — "`packages/integrations/twilio/` client + `from_credential`
    resolver" — outbound HTTPS client + respx tests.
  - `ENG-168c` — "`packages/sms/` controlled-send service + suppression /
    quiet-hours / allowed-template gate" — schema + service + tests.
  - `ENG-169-impl` — "Live-subscriber runtime ADR for Cloud Run" — single
    ADR file under `docs/decisions/`.

## Blockers

- B1 review tail must close before any Wave 2 worker touches
  `packages/tenant/*` (E1 already landed the medium fix; Codex re-review is
  the residual blocker).
- ENG-165 (tenant credential UI/API) — listed as a `blockedBy` on ENG-168
  in Linear; current in-flight diff is the slice satisfying it. Wait for
  Codex acceptance.
- `INTERNAL_CREDENTIAL_TOKEN` must already be wired in prod Secret Manager
  before the Twilio client's `from_credential` resolver path is used from
  any second process — confirmed in tenant CLAUDE.md, but the Twilio ADR
  should call it out explicitly.

## Integration Risks

- ENG-169 ADR risks proposing a new long-running Cloud Run Service shape.
  That conflicts with the 2026-05-15 ENG-172 decision (apps/worker/CLAUDE.md):
  "the long-running `fusion-worker` Cloud Run Service was decommissioned …
  recurring work runs as one-shot Cloud Run Jobs (`fusion-job-bounce-poll`
  every 15min via Cloud Scheduler)". The ADR MUST either reuse that pattern
  or, if it argues for a new Service shape, escalate to user explicitly.
- `tenant.integration_credential.provider_kind = "twilio"` is already in
  `PROVIDER_KINDS` (`packages/tenant/models.py:69`) and
  `ProviderKind` Literal (`packages/tenant/schemas.py:21`). No CHECK-constraint
  migration required for the provider name. Risk: the Twilio Account-SID +
  Auth-Token + (optionally) Messaging-Service-SID payload may not fit
  cleanly into the existing `credential_kind ∈ {oauth_token, api_key,
  password_grant, webhook_secret}` set. The ENG-168a ADR is the place to
  decide; an enum extension needs a separate user-approved migration.
- `packages/outreach` is the closest reference pattern (suppression,
  rate-limit, audit-row hashing, outbox via `outreach.outbound_queue`) but
  SMS must NOT live inside `packages/outreach` per ADR-0004 and the
  cross-package import matrix in `packages/CLAUDE.md`. New `sms` domain is
  required; cross-package import matrix MUST be amended in the same PR that
  adds the package.
- `drain_outbound_queue` is paused in prod per `apps/worker/CLAUDE.md`
  ("ENG-112 reintroduces a real always-on background runtime + Redis
  (Memorystore)"). SMS dispatcher topology (cron-job poll vs always-on
  drain) is exactly the question ENG-169 answers; the SMS service can be
  built before the dispatcher is wired, but the dispatcher worker (W3-T3)
  is gated on ENG-169 + ENG-112.

## Recommended Next Wave

```text
Wave A — doc-only (parallel, ~no scope overlap)
┌─────────────────────────────────────────────────────────────────┐
│ A-T1  ENG-169 ADR: live-subscriber runtime on Cloud Run         │
│       Owner: Claude Code explorer                               │
│       Output: docs/decisions/ADR-XXXX-live-subscriber-runtime.md│
├─────────────────────────────────────────────────────────────────┤
│ A-T2  ENG-168a ADR: Twilio credential payload contract          │
│       Owner: Claude Code explorer                               │
│       Output: docs/decisions/ADR-XXXX-twilio-tenant-credential.md│
├─────────────────────────────────────────────────────────────────┤
│ A-T3  ENG-168c-design ADR: SMS controlled-send architecture     │
│       Owner: Claude Code explorer                               │
│       Output: docs/decisions/ADR-XXXX-sms-controlled-send.md    │
├─────────────────────────────────────────────────────────────────┤
│ A-T4  Codex review of all three ADRs (review-only, no writes)   │
│       Owner: Codex                                              │
└─────────────────────────────────────────────────────────────────┘

Wave B — implementation (sequential within the ENG-168 chain)
┌─────────────────────────────────────────────────────────────────┐
│ B-T1  ENG-168b  packages/integrations/twilio/ client            │
│       Owner: Claude Code worker                                 │
│       Inputs:  A-T2 (Twilio credential ADR) accepted            │
│       Outputs: client + from_credential + respx tests           │
├─────────────────────────────────────────────────────────────────┤
│ B-T2  ENG-168c  packages/sms/ controlled-send service           │
│       Owner: Claude Code worker                                 │
│       Inputs:  A-T3 (SMS ADR) + B-T1 merged                     │
│       Outputs: sms.* schema + service + suppression + tests     │
├─────────────────────────────────────────────────────────────────┤
│ B-T3  Codex migration-safety review of B-T2 alembic revision    │
│       Owner: Codex                                              │
└─────────────────────────────────────────────────────────────────┘

Wave C — dispatcher + workflow wiring (BLOCKED until ENG-169 and ENG-112)
   Not part of the "next wave" — listed here as the visible boundary so the
   orchestrator does not accidentally schedule a worker before its gates clear.
```

### Why this order (and what's the safest first task in each track)

- **Safest first ENG-169 task:** A-T1 — the runtime ADR file itself. The ADR
  enumerates concrete runtime options (Cloud Run Job + Cloud Scheduler [the
  current 2026-05-15 ENG-172 pattern for `fusion-job-bounce-poll`]; HTTP
  receiver Cloud Run Service for outbound-message providers; rejection of
  a new generic always-on worker Service per `PROVIDER_INGESTION_STRATEGY.md`
  §Runtime Implications). It is doc-only and changes no infra; risk is
  bounded to "the chosen option needs revisiting later". The ADR explicitly
  defers `drain_outbound_queue` reintroduction to ENG-112 / Memorystore.

- **Safest first ENG-168 task (after ENG-169 + ENG-168a ADR):** B-T1 —
  `packages/integrations/twilio/` client with `from_credential` + typed
  exceptions + respx-stubbed unit tests. Justification:
  1. Outbound HTTPS only — fits the existing Cloud Run API shape; no
     runtime decision needed (does not depend on the ENG-169 outcome).
  2. No PHI; no `phi.*` imports.
  3. No new migration required (Twilio is already in `PROVIDER_KINDS`).
  4. Mirrors the existing `SfClient` / `CareStackClient` shape — reviewers
     have a clear precedent.
  5. Unblocks B-T2 (`packages/sms/` service) immediately because the SMS
     service will import `TwilioClient.from_credential` at the seam.

Per `feedback_pr_granularity` (solo dev: bundle related slices into one
PR), B-T1 + B-T2 may land as one PR, gated by Codex on the migration
(B-T3) and a strict scope review.

## Per-Task Ownership Plan

### A-T1 — ENG-169 live-subscriber runtime ADR (doc only)

- Agent: Claude Code explorer.
- Write scope:
  - `docs/decisions/ADR-XXXX-live-subscriber-runtime.md` (new file only).
- Read scope:
  - `docs/PROVIDER_INGESTION_STRATEGY.md` (§Runtime Implications is the
    contract this ADR formalises).
  - `docs/DEPLOYMENT_RULES.md`.
  - `docs/decisions/ADR-0002-cloud-run-prod-runtime.md` (worker shape +
    cron pattern + IAP front door already accepted there).
  - `apps/worker/CLAUDE.md` (current ENG-172 state: Cloud Run Jobs +
    Scheduler; `drain_outbound_queue` paused).
  - `apps/worker/main.py` + `apps/worker/jobs/*.py` (current shape).
  - `infra/scripts/deploy_cloud_run.sh`, `infra/scripts/provision_cloud_run_foundation.sh`,
    `infra/scripts/provision_cloud_iap_lb.sh` (existing topology only).
  - `packages/integrations/salesforce/CLAUDE.md` + `client.py` (live-intake
    target #1).
  - `packages/integrations/carestack/CLAUDE.md` + `client.py` (live-intake
    target #2).
- Prohibited files: every `infra/scripts/*.sh`, every `.github/workflows/*.yml`,
  every `infra/env/*`, every Secret Manager / Cloud Run state, every product
  source file, every migration.
- Decisions the ADR MUST make:
  1. Default Cloud Run shape for periodic provider pulls (recommend Cloud
     Run Job + Cloud Scheduler — already validated by `fusion-job-bounce-poll`).
  2. Default Cloud Run shape for outbound-message receivers (recommend Cloud
     Run Service inside the existing api LB + IAP — Salesforce Outbound
     Message / CareStack webhook).
  3. Explicit rejection of a generic always-on `fusion-worker` Service
     (already decommissioned per ENG-172).
  4. Where SMS dispatcher lives once ENG-112 / Memorystore lands (forward-
     reference only; not deciding ENG-112).
  5. Audit / preflight / smoke-test contract that any new Job or Service
     must follow (mirrors deploy-prod's preflight + smoke pattern).
- Stop conditions: stop if the ADR would require Terraform / IaC adoption,
  a new Secret Manager binding, a new VPC connector, or a change to IAP
  bindings — each is a separate user decision.

### A-T2 — ENG-168a Twilio credential payload ADR (doc only)

- Agent: Claude Code explorer; Codex reviewer for sign-off.
- Write scope:
  - `docs/decisions/ADR-XXXX-twilio-tenant-credential.md` (new file only).
- Read scope:
  - `packages/tenant/credential_service.py`,
  - `packages/tenant/models.py` (provider_kind + credential_kind enums),
  - `packages/tenant/schemas.py` (`ProviderKind` Literal, `CredentialKind`),
  - `packages/tenant/CLAUDE.md`,
  - `packages/integrations/CLAUDE.md`,
  - `docs/decisions/ADR-0003-tenant-domain-multi-tenancy.md`,
  - the B1 review report.
- Prohibited files: every `packages/tenant/*.py` source edit, every
  migration under `packages/db/alembic/versions/**`.
- Decisions the ADR MUST make:
  1. Twilio payload shape (Account SID, Auth Token, default From-Number or
     Messaging-Service-SID, optional `status_callback_url`, optional
     `default_country`).
  2. Choice of `credential_kind`. Recommended: reuse `api_key` (Account SID
     + Auth Token are a static credential pair, no refresh). If a new
     `credential_kind` is required (e.g. `twilio_basic`), STOP and surface
     to the user — it is a CHECK-constraint migration and an enum change in
     `models.py` + `schemas.py`.
  3. Where the `default From-Number` lives — inside the encrypted payload,
     or as a non-secret `extra` JSONB. Recommend keeping inside the
     encrypted payload alongside Account SID/Auth Token so the payload is
     a single atomic unit.
  4. Whether `location_id` pinning applies (recommend allowing it, so a
     multi-office clinic can route SMS from per-office numbers).
  5. Audit `extra` shape: tenant_id, credential_id, provider_kind,
     credential_kind, is_default, has_location — NEVER the SID, token, or
     phone number values.
- Stop conditions: any need for a new `credential_kind` enum value, a new
  `provider_kind`, or any non-payload column on
  `tenant.integration_credential` → escalate to user.

### A-T3 — ENG-168c-design SMS controlled-send architecture ADR (doc only)

- Agent: Claude Code explorer.
- Write scope:
  - `docs/decisions/ADR-XXXX-sms-controlled-send.md` (new file only).
- Read scope: `packages/outreach/**` (reference pattern only, no edits),
  `apps/worker/jobs/email_send.py`, `packages/audit/**`,
  `packages/tenant/**`, `docs/governance/TAXONOMY_GOVERNANCE.md`,
  `docs/architecture/CONTEXT_ARCHITECTURE.md`,
  `docs/decisions/ADR-0004-operator-account-email-outreach.md`,
  `packages/CLAUDE.md` (cross-package import matrix).
- Prohibited files: every source file outside the ADR; in particular no
  edits to `packages/outreach/**`.
- Decisions the ADR MUST make:
  1. New top-level domain name: `sms` (recommended) vs `messaging/sms`
     (rejected — too broad; future voice / WhatsApp would justify it but
     not now).
  2. Schema list under `sms.*`:
     - `sms.allowed_template (tenant_id, template_key, body_template,
       category, status)` — allowed-template list, body template stored
       once; the send service never accepts a raw body at the workflow
       boundary.
     - `sms.suppression (tenant_id, recipient_phone_normalised)` — opt-out
       store; composite PK so tenant A's opt-out does not affect tenant B.
       Future-proof for inbound STOP webhook (out of scope for MVP).
     - `sms.send (id, tenant_id, person_uid, template_key, status,
       provider_message_id, idempotency_key, created_at, updated_at)` —
       durable per-recipient send row. Bodies are NOT stored here; bodies
       belong in `interaction.event_content` (M3).
     - `sms.outbound_queue` — mirrors `outreach.outbound_queue` if and
       when ENG-112 lands a real always-on runtime. MVP may omit the queue
       and dispatch synchronously inside the workflow trigger; the ADR
       must state which.
  3. Idempotency key shape: `(tenant_id, person_uid, template_key,
     source_event_id)`.
  4. Quiet-hours storage: `tenant.setting` key `sms.quiet_hours` with a
     typed JSON value (start, end, timezone, exempt_categories).
  5. Audit row shape: tenant_id, credential_id (UUID), template_key,
     recipient_hash (HMAC-SHA256 truncated under `INTERNAL_CREDENTIAL_TOKEN`,
     mirroring outreach), provider_message_id, outcome — NEVER body, NEVER
     plaintext recipient.
  6. Test matrix Wave B will inherit (see "Test Plan" below).
- Stop conditions: any proposal that requires edits inside
  `packages/outreach/**`; any proposal that stores SMS bodies on
  `sms.send`; any new top-level schema name beyond `sms`.

### A-T4 — Codex ADR review pass

- Agent: Codex reviewer.
- Write scope: review notes (Linear comments only — orchestrator handles
  status flips).
- Output: per-ADR pass/fail with concrete fix suggestions.
- Acceptance: all three ADRs must be accepted before B-T1 starts.

### B-T1 — `packages/integrations/twilio/` client (ENG-168b)

- Agent: Claude Code worker.
- Linear: ENG-168b.
- Precondition: A-T2 (Twilio credential ADR) accepted by Codex.
- Write scope (all new files):
  - `packages/integrations/twilio/__init__.py`
  - `packages/integrations/twilio/AGENTS.md`
  - `packages/integrations/twilio/CLAUDE.md`
  - `packages/integrations/twilio/client.py` — async `TwilioClient` with
    `from_credential(IntegrationCredentialOut, *, default_from: str | None = None)`,
    `send_message(to, body, *, idempotency_key)` returning a typed
    `TwilioSendResult`, and the same 401/5xx mapping pattern as `SfClient`
    / `CareStackClient`.
  - `packages/integrations/twilio/exceptions.py` — `TwilioNotConnectedError`,
    `TwilioApiError`, `TwilioRateLimitError` (mirrors the outreach send
    error taxonomy: `RateLimitError`, `TransientError`, `PermanentError`).
  - `tests/integrations/twilio/__init__.py`
  - `tests/integrations/twilio/test_client.py` (respx-stubbed).
- Read scope: `packages/integrations/**` (reference patterns),
  `packages/integrations/CLAUDE.md`, `packages/tenant/credential_service.py`
  (read-only consumer of `IntegrationCredentialService.read_for`), the
  A-T2 ADR.
- Prohibited files: every `packages/tenant/*.py` source edit; every
  migration; every workflow / env / deploy file; every other source
  directory.
- Expected tests (must pass `.venv/bin/python -m pytest
  tests/integrations/twilio/`):
  - `from_credential` happy path: payload with `account_sid`, `auth_token`,
    `default_from` resolves and stores correctly on the client.
  - `from_credential` rejects when payload missing required field →
    `TwilioNotConnectedError`.
  - `send_message` happy path: 201 response → `TwilioSendResult` with
    `provider_message_id` populated.
  - `send_message` 401 → `TwilioNotConnectedError` (no auto-reauth; static
    credential).
  - `send_message` 429 → `TwilioRateLimitError` (transient; carries
    `retry_after_seconds`).
  - `send_message` 5xx → `TwilioApiError`.
  - `send_message` adds the `Idempotency-Key` HTTP header from the
    `idempotency_key` arg (Twilio honours this on outbound message create).
- Stop conditions: stop if implementation needs a new `credential_kind`
  enum value, a new `provider_kind`, or any PHI import. Stop if Twilio's
  outbound `Messages` API has changed and the spec the ADR captured is
  stale — escalate to user.

### B-T2 — `packages/sms/` controlled-send service (ENG-168c)

- Agent: Claude Code worker.
- Linear: ENG-168c.
- Precondition: A-T3 (SMS ADR) + B-T1 merged. May be opened as the same PR
  as B-T1 per `feedback_pr_granularity`.
- Write scope (all new files unless otherwise noted):
  - `packages/sms/__init__.py`
  - `packages/sms/AGENTS.md`
  - `packages/sms/CLAUDE.md`
  - `packages/sms/models.py` (`sms.allowed_template`, `sms.suppression`,
    `sms.send`, and `sms.outbound_queue` only if the A-T3 ADR keeps it for
    MVP).
  - `packages/sms/repository.py`
  - `packages/sms/schemas.py`
  - `packages/sms/service.py` — `SmsService` public surface:
    `send_via_template(tenant_id, person_uid, template_key, *,
    idempotency_key, source_event_id)`. Gates in order: credential
    existence → opt-out → quiet-hours → allowed-template lookup → rate
    limit → render → call `TwilioClient.send_message` → audit row →
    persist `sms.send`. Mirrors `SendService.enqueue_single` discipline.
  - `packages/sms/consent.py` — `is_opted_out`, `add_suppression`,
    `remove_suppression`. No inbound webhook handler in MVP (future
    Twilio `messageReceived` route is a separate ticket).
  - `packages/db/alembic/versions/<timestamp>_<rev>_sms_v1.py` — new
    revision. Never edits a merged migration; chains linearly off the
    current `head`.
  - `tests/sms/__init__.py`
  - `tests/sms/test_service.py`, `tests/sms/test_consent.py`,
    `tests/sms/test_repository.py`.
- Read scope: `packages/outreach/**` (reference pattern, no imports),
  `packages/tenant/**`, `packages/audit/**`, `packages/identity/**`
  (recipient phone resolution via `normalise_phone`), the A-T3 ADR.
- Prohibited files:
  - `packages/outreach/**` (must not edit, must not import — ADR-0004
    keeps outreach email-only).
  - `packages/integrations/twilio/**` (B-T1 owns it; B-T2 imports only).
  - `packages/phi/**` (no PHI in this domain).
  - `packages/CLAUDE.md` import matrix MUST be amended in the same PR to
    add an `sms` row + an `sms` column. That edit is in scope.
- Expected tests (must pass `.venv/bin/python -m pytest tests/sms/`):
  - missing credential → `SmsCredentialMissing` error.
  - revoked credential → same error path; credential resolver filters
    `status='active'`.
  - opt-out hit (`sms.suppression`) → no provider call, audit row
    `sms.send.suppressed`.
  - quiet-hours window block → no provider call, audit row
    `sms.send.deferred_quiet_hours`.
  - allowed-template lookup miss → `SmsTemplateNotAllowed` error.
  - rate-limit deferral (mocked rate limiter) → audit row
    `sms.send.rate_limited`.
  - idempotency-key collision (duplicate workflow event) → returns the
    existing `sms.send` row, no second provider call.
  - audit row shape sanitiser: never contains body, contains hashed
    recipient (mirror outreach's `recipient_hash`).
  - success path: provider returns 201 → `sms.send` row in `sent`,
    `provider_message_id` populated, audit row `sms.send.sent` written.
- Stop conditions:
  - schema proposed would store full SMS body → STOP, bodies belong in
    `interaction.event_content` (M3).
  - new top-level schema name needed beyond `sms` → STOP, escalate.
  - any need to import `packages/outreach/**` → STOP.
  - any need to mutate the cross-package import matrix beyond adding the
    `sms` row/column → STOP.

### B-T3 — Codex migration-safety review of B-T2 alembic revision

- Agent: Codex.
- Checks: `alembic upgrade head` clean from current head, `alembic
  downgrade -1` clean, no edits to any merged revision, linear chain
  (single `down_revision`), DB schema constraints match
  `packages/CLAUDE.md` naming.
- Write scope: review comments only.

## Dependencies and Gates

```text
B1 close (Codex re-review of tenant credential diff)
    │
    ▼
ENG-165 acceptance (current tenant credential bundle)
    │
    ▼
A-T1 (ENG-169 runtime ADR)   ──┐
A-T2 (ENG-168a credential ADR)─┼─► A-T4 (Codex review of all ADRs)
A-T3 (ENG-168c-design ADR)   ──┘             │
                                              ▼
                                  B-T1 (Twilio client, ENG-168b)
                                              │
                                              ▼
                                  B-T2 (SMS service, ENG-168c)
                                              │
                                              ▼
                                  B-T3 (Codex migration review)
                                              │
                                              ▼
        BLOCKED — Wave C dispatcher + workflow wiring
        gated on ENG-169 outcome + ENG-112 (Memorystore)
```

Hard gates (do NOT skip):

1. B1 Codex re-review accepts E1's tenant fix before any new worker writes
   to `packages/tenant/*`.
2. ENG-165 (current tenant credential UI/API slice) merged to `main`.
3. ENG-169 ADR accepted by Codex AND user (runtime decisions are
   architectural per the root CLAUDE.md workflow rules).
4. ENG-168a Twilio credential ADR accepted by Codex.
5. ENG-168c-design SMS ADR accepted by Codex AND user (introduces a new
   top-level domain `sms` — architectural change, per the root CLAUDE.md
   workflow rule "Before structural changes … propose first, implement
   after approval").
6. `INTERNAL_CREDENTIAL_TOKEN` confirmed present in production Secret
   Manager bindings (already required for the multi-process resolver; the
   Twilio ADR should call it out so the worker that runs the SMS service
   inherits the same binding).

## What Must Be Blocked Until User Approval or Design Decision

1. **Any new long-running Cloud Run Service** for live ingestion or SMS
   dispatch. ENG-172 decommissioned the generic worker Service; reviving
   that shape needs explicit user approval (and probably ENG-112 +
   Memorystore first).
2. **Any extension of `credential_kind`** beyond `{oauth_token, api_key,
   password_grant, webhook_secret}`. Adding a value is a CHECK-constraint
   migration and an enum widening in `models.py` + `schemas.py` — user
   approval required.
3. **Inbound Twilio webhook handler** (STOP / HELP / UNSTOP / inbound MMS).
   Out of scope for ENG-168 MVP. The `sms.suppression` schema must be
   future-proof for it, but the route must NOT be added in this wave.
4. **Any write into `phi.*` outside `PhiService`**. Specifically: the
   CareStack appointment poller (out of scope for this wave; gated by
   ENG-167) must not be implemented in the SMS service.
5. **Taxonomy / semantic label additions** per
   `docs/governance/TAXONOMY_GOVERNANCE.md`. Allowed-template categories
   must be drawn from the existing taxonomy; new labels need approval.
6. **Provider write-back** (outbound POST/PATCH to Salesforce, CareStack,
   or any tenant-owned provider). Blocked by
   `feedback_production_readonly_pull`.
7. **Cloud Scheduler / Cloud Run Job creation, IAM grants, Secret Manager
   secret creation**. All `gcloud` mutations are off-limits to workers in
   this mission. The ADR can recommend topology but cannot apply it.
8. **Migrations that touch already-merged revisions**. Hard rule from
   root CLAUDE.md. The B-T2 migration adds a new revision; it does not
   edit any existing one.
9. **Any change to the cross-package import matrix beyond adding the
   `sms` row/column** in `packages/CLAUDE.md`. Other edits there require
   explicit user approval — the matrix is a hard architectural invariant.
10. **Body persistence on `sms.send`**. ADR-decided: bodies live in
    `interaction.event_content` (M3), never in `sms.*`. Any draft that
    proposes to store the body must STOP.

## Test Plan (required before integration)

### Wave A (ADRs, doc-only)

- No code tests required.
- Each ADR MUST include:
  - "Decision" section (concrete, one-line answers to each question).
  - "Consequences" section (cost, blast radius, what gets unblocked).
  - "Alternatives considered" with explicit "rejected because" reasons.
  - Test matrix that Wave B will inherit (ENG-168c-design ADR only).

### B-T1 — Twilio client

- `.venv/bin/python -m pytest tests/integrations/twilio/` → 7 tests above
  must pass.
- `python -m pytest tests/integrations/twilio/ -k from_credential` must
  exercise both happy and missing-field paths.
- No live Twilio traffic; respx-only.

### B-T2 — SMS service

- `.venv/bin/python -m pytest tests/sms/` → 9 tests above must pass.
- `alembic upgrade head && alembic downgrade -1 && alembic upgrade head`
  must run clean against the test database (real PostgreSQL per root
  CLAUDE.md "Integration tests must use a real PostgreSQL").
- Audit-row sanitiser test must explicitly assert no body, no plaintext
  recipient, hashed recipient present.
- `verify` skill (lint + typecheck + tests + alembic drift) must pass on
  the full bundled PR before requesting Codex review.

### Wave B PR-level

- Migration ordering verified (Codex B-T3): the SMS revision is the only
  Wave B migration; no other concurrent revision exists.
- Cross-package import matrix update is in the same PR as the new `sms`
  package.
- No edits to `packages/outreach/**`, `packages/tenant/*.py` source files,
  `packages/integrations/twilio/**` after B-T1 lands (single-owner
  discipline).

## Risks

| Risk | Likelihood | Blast radius | Mitigation |
|---|---|---|---|
| ENG-169 ADR proposes a new always-on Cloud Run Service | medium | reopens decommissioned ENG-172 pattern | ADR template forces an "alternatives considered" review of the Cloud Run Job + Scheduler pattern that already works (`fusion-job-bounce-poll`). Codex MUST reject any draft that picks "new Service" without explicit user sign-off. |
| Twilio payload contract churns mid-implementation | medium | requires re-encrypting all stored credentials | A-T2 ADR + Codex sign-off BEFORE B-T1 lands. The payload shape is the only thing that requires re-encryption — the schema does not. |
| Worker tries to write into `packages/tenant/credential_service.py` directly | low (after B1) | concurrent dirty-file conflict with E1's still-unmerged work | Wave A is doc-only. Wave B's B-T1 + B-T2 read `IntegrationCredentialService` via import only; never edits it. The tenant credential bundle (B1 / E1 review tail) must close first. |
| SMS service grows into `packages/outreach` by accident | medium | violates ADR-0004 + import matrix | A-T3 ADR explicitly rejects this. B-T2 worker has prohibited-files list. Codex enforces. |
| Migration conflicts with an in-flight ENG-165 follow-up migration | low | linear-chain violation | Sequence: ENG-165 fully merged BEFORE B-T2 opens its alembic revision. The orchestrator gates this. |
| `INTERNAL_CREDENTIAL_TOKEN` not propagated to the SMS-dispatching process | low | broken credential resolution in prod | A-T1 ADR section on "audit / preflight contract" must require this env var on every new Job / Service it sanctions. |
| ENG-167 / ENG-166 workers try to invoke SMS before Wave C is unblocked | medium | unaudited side effects + unsafe topology | A-T1 ADR + A-T3 ADR both state explicitly: workflow trigger (W3-T3 from C1's Wave 3) is the ONLY allowed caller path; agents and direct service calls are out of scope. Stop conditions in worker briefs enforce. |
| PHI accidentally logged by SMS code path | low | PHI breach class incident | A-T3 ADR's audit-row contract: hashed recipient only, no body. B-T2 has an explicit audit sanitiser test. |

## Process / Lesson Notes

- C1's recommended next-task list (in its `Recommendations` section) is
  already a defensible Wave-A plan. G1 collapses it into a single
  doc-only Wave A + a single implementation-bundle Wave B per
  `feedback_pr_granularity`, and explicitly carries forward the gates
  that A2-live, B1, D1, E1, F1 surfaced (B1 re-review tail; ENG-172
  decommission state of the worker Service).
- ENG-168 implementation does NOT strictly depend on ENG-169 ADR
  (Twilio is outbound HTTPS, not a subscriber). The wave is sequenced
  ENG-169 → ENG-168 because the user grouped them that way in the
  brief and because the ENG-169 ADR also informs where the future SMS
  dispatcher lives (Wave C, blocked).
- The `packages/sms` domain introduction is structural; per the root
  CLAUDE.md workflow rule "Before structural changes … propose first,
  implement after approval", the A-T3 ADR is the proposal vehicle.
  Codex sign-off alone is insufficient; the user must accept the new
  domain.
- All three Wave A ADRs MUST be created with the existing
  `docs/decisions/ADR-0000-template.md` template so numbering stays
  consistent.

## Suggested Next Tasks

In execution order:

1. Codex reviews this G1 brief (read-only) and posts a Linear comment
   on ENG-169 + ENG-168 + ENG-168a/b/c.
2. Spawn Claude Code explorer for A-T1 (ENG-169 ADR). Output: one
   ADR file under `docs/decisions/`.
3. In parallel: spawn Claude Code explorer for A-T2 (Twilio credential
   ADR) and A-T3 (SMS controlled-send ADR). Each output: one ADR file.
   Write scopes are disjoint.
4. Codex A-T4 review pass on all three ADRs. User accepts ENG-169
   topology + new `sms` domain.
5. B1 / E1 tenant credential bundle merged to `main` (existing
   mission tail; not part of this wave, but is a gate).
6. ENG-165 acceptance.
7. Spawn Claude Code worker for B-T1 + B-T2 as one bundled PR per
   `feedback_pr_granularity`. Gate on Codex B-T3 migration review.
8. Pause. Wave C (workflow trigger + dispatcher wiring) is BLOCKED
   on ENG-169 outcome and ENG-112 (Memorystore). The orchestrator
   re-plans after Wave B closes.
