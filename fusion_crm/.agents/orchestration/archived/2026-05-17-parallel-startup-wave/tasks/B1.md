# Terminal Agent Task Brief

Task ID: B1
Linear issues: ENG-177 / ENG-165
Agent role: Codex reviewer
Mission folder: `.agents/orchestration/20260517-113000-parallel-startup-wave`
Report path: `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/B1.md`
Branch: main
Worktree: primary repo

## Objective

Review the existing dirty tenant credential diff and decide whether it is ready to continue as ENG-177 / ENG-165 work, needs fixes, or should be split into smaller tasks.

## Ownership

Allowed write scope:
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/B1.md`
- mission board/coordination files when updating status.

Allowed read scope:
- `apps/api/routers/tenant.py`
- `packages/tenant/credential_service.py`
- `packages/tenant/schemas.py`
- `tests/tenant/test_credential_service.py`
- `tests/api/test_tenant_credential_routes.py`
- local `CLAUDE.md` files for `apps/api` and `packages/tenant`.

Do not touch:
- Tenant source/test files during review.
- Deploy workflow files.
- `.env*`, secrets, migrations.

## Review Questions

- Does the diff correctly hide revoked credentials from default operator UI/API paths?
- Does `include_revoked` behave safely for default setting and admin listing?
- Does metadata update avoid decrypting or logging payload values?
- Are audit rows complete and sanitized?
- Do route handlers preserve “no business logic in routes” enough for this codebase?
- Are tests sufficient? What focused tests should run next?
- Is this one task or should it be split into ENG-177 fix vs ENG-165 credential-edit API?

## Required Report

Use code-review stance: findings first, ordered by severity with file/line references, then open questions, then tests/checks, then recommended next task.
