# Task S2 — ENG-185 Salesforce Cutover Planner

## Role

Claude Code read-only explorer.

You are not alone in the codebase. S1 may be editing `packages/identity/**` in
parallel. Do not edit product files. Write only the report path below.

## Goal

Prepare the smallest safe follow-up wave that cuts Salesforce ingest over to
the S1 identity match policy entry point after S1 is reviewed.

## Linear

- ENG-185

## Required Reading

- `CLAUDE.md`
- `packages/CLAUDE.md`
- `packages/ingest/CLAUDE.md`
- `packages/identity/CLAUDE.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/Q1.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R1.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R2.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/tasks/S1.md`

## Owned Write Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/S2.md`

## Read Scope

- `packages/ingest/**`
- `packages/identity/**`
- `packages/integrations/salesforce/**`
- `tests/ingest/**`
- `tests/identity/**`
- `tests/api/test_integrations_salesforce.py`

## Out Of Scope

- Product file edits.
- Migrations.
- Linear mutation.
- Deployment, env vars, secrets, GitHub Actions, Cloud Run, commits, pushes,
  merges, rebases.

## Analysis Requirements

Identify:

- the exact changes needed in `packages/ingest/sf_lead_service.py` after S1;
- how to call `IngestService.capture_normalized_person_hint(...)` and adapt
  the R1 DTO into S1's identity-owned `MatchHintIn`;
- the tests in `tests/ingest/test_sf_lead_service.py` that must be rewritten;
- how `ops.lead.extra.is_reactivation` should map from `ResolveFromHintResult`;
- idempotency expectations for re-pull, source link, hint, and match candidate;
- risks around PHI/raw payload, tenant isolation, and open candidates not
  blocking the pull;
- an owned write scope and verification gate for the future Salesforce cutover
  wave.

If S1 has not reported by the time you finish, base the plan on `tasks/S1.md`
and mark final contract verification pending.

## Required Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/S2.md`
with:

- current Salesforce ingest flow summary;
- proposed cutover steps;
- write scopes and sequencing;
- test rewrite plan;
- verification commands;
- blockers/dependencies on S1.
