# Task T2 — Wave T Verification Scout

## Role

Claude Code read-only verifier / reviewer.

You are not alone in the codebase. T1 may be editing
`packages/ingest/sf_lead_service.py`, `tests/ingest/test_sf_lead_service.py`,
and `packages/ingest/CLAUDE.md` in parallel. Do not edit product files. Write
only the report path below.

## Goal

Scout verification risks for the Salesforce cutover wave while T1 runs. Focus
on ensuring the cutover consumes S1's identity contract without widening scope.

## Owned Write Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/T2.md`

## Read Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/**`
- `packages/ingest/**`
- `packages/identity/**`
- `tests/ingest/**`
- `tests/identity/**`
- `tests/api/test_integrations_salesforce.py`

## Out Of Scope

- Product file edits.
- Migrations.
- Linear mutation.
- Deployment, env vars, secrets, GitHub Actions, Cloud Run, commits, pushes,
  merges, rebases.
- Migration upgrade/downgrade.

## Verification Scout Requirements

Check and report:

- whether T1's write scope is disjoint from S1 identity files and from future
  `ops.inquiry`;
- whether the SF service still captures raw events before normalization;
- whether the SF service no longer reaches into `IdentityService._repo`;
- whether raw SOQL records stay out of identity;
- whether `is_reactivation` mapping preserves S2 semantics;
- focused commands Codex should run after T1;
- whether `reports/T1.md` exists by the time you finish. If it exists, include
  a brief read-only summary; if not, mark T1 verification pending.

Do not run destructive git commands. Do not run migration upgrade/downgrade.
Read-only commands and focused non-mutating tests are allowed.

## Required Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/T2.md`
with:

- T1 report availability;
- ownership/contract observations;
- cutover behavior risk observations;
- recommended Codex review and verification checklist;
- blockers.
