# Task S3 — Wave S Verification Scout

## Role

Claude Code read-only verifier / reviewer.

You are not alone in the codebase. S1 may be editing `packages/identity/**` in
parallel. Do not edit product files. Write only the report path below.

## Goal

Scout verification risks for Wave S while S1/S2 run. Focus on whether the
identity-only match policy entry point can be reviewed safely before any
Salesforce/CareStack cutover or new migration writer wave.

## Owned Write Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/S3.md`

## Read Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/**`
- `packages/identity/**`
- `packages/ingest/**`
- `packages/db/alembic/versions/**`
- `tests/identity/**`
- `tests/ingest/**`

## Out Of Scope

- Product file edits.
- Migrations.
- Linear mutation.
- Deployment, env vars, secrets, GitHub Actions, Cloud Run, commits, pushes,
  merges, rebases.
- Migration upgrade/downgrade.

## Verification Scout Requirements

Check and report:

- whether S1's proposed write scope is disjoint from R1 and future Salesforce
  cutover scopes;
- whether S1 can stay migration-free and keep `alembic check` clean;
- import-boundary risks, especially `identity` importing `ingest`;
- PHI/raw-payload guard expectations for `MatchHintIn`, evidence, and
  conflicts;
- tenant isolation and idempotency risks in the planned repository/service
  methods;
- focused commands Codex should run after S1;
- whether `reports/S1.md` exists by the time you finish. If it exists, include
  a brief read-only summary; if not, mark S1 verification pending.

Do not run destructive git commands. Do not run migration upgrade/downgrade.
Read-only commands and focused non-mutating tests are allowed.

## Required Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/S3.md`
with:

- S1 report availability;
- ownership/contract observations;
- import and migration risk observations;
- recommended Codex review and verification checklist;
- blockers.
