# Task R3 — Wave R Verification Scout

## Role

Claude Code read-only verifier / reviewer.

You are not alone in the codebase. R1 may be editing `packages/ingest/*` and a
new Alembic revision in parallel. Do not edit product files. Write only the
report path below.

## Goal

Scout verification risks for Wave R while R1/R2 run. Focus on whether Q1's
`identity.match_candidate` foundation and the planned R1
`ingest.normalized_person_hint` slice can coexist cleanly in the Alembic chain,
ownership model, and focused test plan.

## Owned Write Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R3.md`

## Read Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/**`
- `packages/db/alembic/versions/**`
- `packages/identity/**`
- `packages/ingest/**`
- `tests/identity/**`
- `tests/ingest/**`

## Out Of Scope

- Product file edits.
- Migrations.
- Linear mutation.
- Deployment, env vars, secrets, GitHub Actions, Cloud Run, commits, pushes,
  merges, rebases.

## Verification Scout Requirements

Check and report:

- current Alembic head and whether Q1's revision is the expected parent for R1;
- whether R1's proposed write scope is disjoint from Q1 except for migration
  chain dependency;
- focused commands Codex should run after R1;
- any likely ownership or contract risk before Codex review;
- whether `reports/R1.md` exists by the time you finish. If it exists, include
  a brief read-only summary; if not, mark R1 verification pending.

Do not run destructive git commands. Do not run migration upgrade/downgrade.
Read-only commands and focused non-mutating tests are allowed.

## Required Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R3.md`
with:

- status of R1 report availability;
- Alembic/migration-chain observations;
- ownership/contract observations;
- recommended Codex review and verification checklist;
- blockers.
