# Task E1: Tenant Credential Default Status Edge Case

## Role

Claude Code worker.

You are not alone in the codebase. Other workers and Codex may have active changes in different files. Do not revert edits made by others; adapt to them.

## Goal

Fix the medium finding from `reports/B1.md`: `IntegrationCredentialService.update_metadata` can leave a non-active credential marked as default when an active default is updated to `status="expired"` without explicitly sending `is_default=False`.

## Required Reading

- `CLAUDE.md`
- `packages/tenant/CLAUDE.md`
- `apps/api/CLAUDE.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/B1.md`

## Write Scope

You may edit only:

- `packages/tenant/credential_service.py`
- `tests/tenant/test_credential_service.py`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/E1.md`

## Implementation

- Make `update_metadata` clear `cred.is_default` when the resulting credential status is not `active`.
- Preserve the existing behavior that `is_default=True` is rejected for non-active credentials.
- Add a focused service test showing that a default active credential updated to `status="expired"` ends with `is_default is False`.
- Do not edit API routes, schemas, migrations, env files, commits, or Linear.

## Verification

Run:

```bash
.venv/bin/python -m pytest tests/tenant/test_credential_service.py
```

If that fails because `.venv` is unavailable, run the closest available pytest command and report the environment failure.

## Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/E1.md` with:

- files changed;
- exact tests run and result;
- any blocker or assumption;
- confirmation that no production operation was performed.
