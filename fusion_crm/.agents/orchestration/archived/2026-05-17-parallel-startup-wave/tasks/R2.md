# Task R2 — ENG-185 Follow-up Pipeline Integration Plan

## Role

Claude Code read-only explorer.

You are not alone in the codebase. R1 may be editing `packages/ingest/*` and a
new Alembic revision in parallel. Do not edit product files. Write only the
report path below.

## Goal

Prepare the smallest safe ENG-185 implementation plan for replacing hidden
Salesforce email/phone reactivation matching with the explicit
`ingest.normalized_person_hint` + `identity.match_candidate` flow.

## Linear

- ENG-185

## Required Reading

- `CLAUDE.md`
- `packages/CLAUDE.md`
- `packages/ingest/CLAUDE.md`
- `packages/identity/CLAUDE.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/P2-data-foundation-implementation-plan.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/Q1.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/tasks/R1.md`

## Owned Write Scope

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R2.md`

## Read Scope

- `packages/ingest/**`
- `packages/identity/**`
- `packages/integrations/salesforce/**`
- `tests/ingest/**`
- `tests/identity/**`
- `tests/api/test_integrations_salesforce.py`

## Out Of Scope

- Product file edits.
- Migrations.
- API route changes.
- Deployment, env vars, secrets, GitHub Actions, Cloud Run, commits, pushes,
  merges, rebases.

## Analysis Requirements

Identify:

- the current hidden matching path in Salesforce ingest;
- the exact service boundary where a future
  `IdentityService.resolve_or_create_from_hint(...)` should live;
- which existing tests need to move or be rewritten;
- the smallest safe ENG-185 write scope after R1 lands;
- risks around PHI/raw payload, tenant isolation, idempotency, and source-link
  behavior;
- acceptance criteria and focused verification commands.

Do not assume R1 is complete. If R1 output is not present yet, base the plan on
P2/Q1 and note the dependency on R1 report review.

## Required Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/R2.md`
with:

- current flow summary;
- proposed ENG-185 implementation steps;
- owned write scopes and sequencing;
- tests/checks to run;
- blockers/dependencies on R1;
- any contract changes needed before implementation.
