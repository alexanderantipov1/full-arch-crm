# Task H1: Production Salesforce Integration Read-Only Triage

## Role

Claude Code explorer / diagnostician.

You are not alone in the codebase. Other workers and Codex may have active changes in different files. Do not revert edits made by others; adapt to them.

## Goal

Triage the user's report that Salesforce integration is not working in production. This task is read-only and must not mutate production or repository product files.

## Required Reading

- `CLAUDE.md`
- `apps/api/CLAUDE.md`
- `apps/web/CLAUDE.md`
- `docs/DEPLOYMENT_RULES.md`
- `packages/integrations/salesforce/CLAUDE.md`
- relevant Salesforce route/client files.

## Write Scope

You may edit only:

- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/H1.md`

## Allowed Read-Only Diagnostics

- Read code and tests.
- Run targeted `rg`, `sed`, `git diff`, `git status`.
- Read production logs using `gcloud logging read` only.
- Read Cloud Run service/revision metadata using `gcloud run services describe` / `gcloud run revisions list` only.
- Read recent GitHub Actions metadata/logs using `gh run list/view` only.

## Prohibited

- No `gcloud run deploy`, `update`, `jobs execute`, traffic changes, IAM/secrets/env changes.
- No GitHub Actions rerun/cancel/dispatch.
- No commit, push, branch switch, or destructive git command.
- Do not print token/secret/credential payload values. If logs contain secrets, redact them.
- Do not edit product files.
- Do not mutate Linear.

## Questions To Answer

- What exact production surface appears broken: OAuth start/callback, integration status/list, token refresh, lead raw/sync, or another route?
- Are there recent prod logs showing `sf.oauth`, `salesforce`, `sf_not_connected`, `invalid_grant`, callback errors, 4xx/5xx, or route shadowing?
- Does current local diff plausibly fix or affect the Salesforce prod issue?
- What is the safest next action: local code fix, prod log follow-up, user reproduction detail, or approved deploy?

## Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/H1.md` with:

- commands run;
- evidence found;
- likely root cause(s), clearly separating evidence from hypothesis;
- recommended next step;
- confirmation that no production mutation was performed.
