# Task D1: Deploy Smoke IAP Email Claim

## Role

Claude Code worker.

You are not alone in the codebase. Other workers and Codex may have active changes in different files. Do not revert edits made by others; adapt to them.

## Goal

Implement the next deploy-smoke fix identified by `reports/A2-codex-followup.md`: the impersonated service-account OIDC token used for the IAP deep smoke must include an email claim.

## Required Reading

- `CLAUDE.md`
- `.github/CLAUDE.md`
- `docs/DEPLOYMENT_RULES.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/A2-live.md`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/A2-codex-followup.md`

## Write Scope

You may edit only:

- `.github/workflows/deploy-prod.yml`
- `tests/core/test_deploy_prod_smoke_logging.py`
- `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/D1.md`

## Implementation

- Add `--include-email` to the `gcloud auth print-identity-token` command in the deploy-prod `api-smoke` step.
- Keep the existing `--impersonate-service-account="${DEPLOYER_SA}"` and `--audiences="${IAP_AUDIENCE}"`.
- Update the static workflow regression test to assert that the smoke token command includes `--include-email`.
- Do not rerun GitHub Actions.
- Do not deploy, mutate Cloud Run, edit secrets/env, commit, push, or move Linear.

## Verification

Run:

```bash
python -m pytest tests/core/test_deploy_prod_smoke_logging.py
```

If the system Python lacks dependencies, retry with:

```bash
.venv/bin/python -m pytest tests/core/test_deploy_prod_smoke_logging.py
```

## Report

Write `.agents/orchestration/20260517-113000-parallel-startup-wave/reports/D1.md` with:

- files changed;
- exact tests run and result;
- any blocker or assumption;
- confirmation that no production operation was performed.
