You are a Fusion CRM worker agent.

Task:
- Task id: ENG-190
- Linear issue: ENG-190
- Linear URL: https://linear.app/fusion-dental-implants/issue/ENG-190/source-instance-scoping-for-provider-and-import-external-ids
- Linear title: Source instance scoping for provider and import external IDs
- Runtime: codex
- Session id: 4ee425484fbc

Rules:
- Read CLAUDE.md, AGENTS.md, .agents/CLAUDE.md, .agents/AGENTS.md, and .agents/orchestration/CLAUDE.md.
- Keep repository files in English.
- Do not edit .env* or shipped Alembic revisions.
- Do not commit, push, or run destructive commands.
- Keep work inside the task scope.
- Keep dashboard state current by updating .agents/orchestration/current/runtime.json and .agents/orchestration/current/runlog.md during long work.
- Write the final or paused report to .agents/orchestration/current/reports/ENG-190-worker-report.md.
- Include changed files, tests run, verification result, risks, blockers, and do-not-merge conditions in the report.

Assigned work:
Implement ENG-190 in the current worktree. You are not alone in the codebase; do not revert or overwrite unrelated edits, and adjust to existing WIP instead of undoing it.

Required reading before edits:
- CLAUDE.md and AGENTS.md
- packages/CLAUDE.md, packages/db/CLAUDE.md, packages/core/CLAUDE.md, packages/identity/CLAUDE.md, packages/ingest/CLAUDE.md
- docs/DEPLOYMENT_RULES.md only if you touch Settings/env/deploy paths
- docs/architecture/IDENTITY_RELATIONSHIP_GRAPH.md
- docs/data-model/CATALOG.md
- packages/identity/models.py, repository.py, service.py, schemas.py
- packages/ingest/service.py and schemas.py
- existing identity/ingest tests around source_link and resolve_or_create_from_hint

Goal:
Add first-class source_instance scoping for provider/import external IDs so two source instances can emit the same source_system/source_kind/source_id without colliding. ENG-183 must remain blocked; do not implement ops.inquiry.

Expected implementation direction from preflight:
- Add a non-null source_instance column to identity.source_link, defaulting existing rows to an explicit single-instance value where needed.
- Replace the old uniqueness semantics with uniqueness on tenant_id + source_system + source_instance + source_kind + source_id when source_id is not null. Preserve tenant scoping.
- Update repository/service lookup and add-source paths so source-link recapture includes source_instance.
- Thread source_instance through DTOs/services where source links are created or looked up, including ingest-side source-data/dev projections where relevant.
- Preserve existing single-instance Salesforce/CareStack behavior with stable defaults such as salesforce-main/carestack-main only where the caller cannot yet provide a better instance.
- Update docs: packages/identity/CLAUDE.md and docs/data-model/CATALOG.md.
- Create a new Alembic revision; do not edit shipped revisions.
- Add/adjust tests covering same tenant/system/kind/source_id from two source instances resolving to distinct source links/persons unless identity policy explicitly matches them.

Verification target:
- Focused identity/ingest tests for changed behavior.
- make lint
- uv run mypy .
- make test
- cd packages/db && env -u SECRET_KEY -u REDIS_URL DATABASE_URL_SYNC=postgresql+psycopg://fusion:fusion@127.0.0.1:5434/fusion PYTHONPATH=../.. uv run alembic check

Report:
Write .agents/orchestration/current/reports/ENG-190-worker-report.md with changed files, tests run, verification status, risks, blockers, and do-not-merge conditions. Keep runtime/runlog heartbeats current during long work.
