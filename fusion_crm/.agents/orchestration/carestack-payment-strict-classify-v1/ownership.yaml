mission: carestack-payment-strict-classify-v1
linear_anchor: ENG-284
owner_role: orchestrator
owner_agent: claude-code
depends_on: ENG-283 (transactionCode classification — this corrects its over-broad isReversed)
tasks:
  - task_id: ENG-284
    title: "Strict payment-code classification — fix isReversed + negative Collected"
    linear_id: ENG-284
    linear_url: https://linear.app/fusion-dental-implants/issue/ENG-284/strict-payment-code-classification-fix-over-broad-isreversed-negative
    worker_role: worker
    worker_agent: claude-code
    workspace: worktree
    owns:
      - packages/db/alembic/versions/   # corrective migration: delete spurious + reclassify
      - packages/ingest/carestack_accounting_transaction_service.py
      - tests/
notes:
  - "Strict payment-code allowlist; non-payment codes never produce a payment event, even isReversed."
  - "Migration DELETEs spurious payment events + reclassifies (append-only exception)."
  - "Orchestrator restarts local arq worker on new code after merge to stop re-pollution."
