# Decision Log — carestack-payment-strict-classify-v1

- 2026-05-30 | Handoff: orchestrator/claude-code -> worker/claude-code for ENG-284.
- 2026-05-30 | Root cause: ENG-283 isReversed override promoted reversed non-payment codes (charges/adjustments) to payment_reversed → collected formula subtracted ~$110k → negative.
- 2026-05-30 | Decision: strict payment-code allowlist; non-payment codes never produce payment events. isReversed flips only payment codes. Corrective migration DELETEs spurious payment events + reclassifies. Aggregate formula kept (now correct).
- 2026-05-30 | Orchestrator killed the stale local arq worker (old code re-emitting PATPAYMENTAPPLIED as payment_recorded); will restart on new code after merge.
- 2026-05-30 | Worker | Append-only exception: corrective migration `c7d8e9f0a1b2` mutates `interaction.event` (DELETE + UPDATE). Same precedent as ENG-269 dedup DELETE, ENG-270 location-id backfill UPDATE, ENG-283 classify UPDATE. Runtime `InteractionService` stays append-only. `downgrade()` is a documented no-op (cannot restore deleted rows; reversing the UPDATE would reinstate known-wrong ENG-283 classifications).
- 2026-05-30 | Worker | `Needs decision:` resolved in favour of the DoD aggregate target. The orchestrator prompt's literal "isReversed=true reclassifies to payment_reversed ONLY when the code is already a payment code" would also flip PATPAYMENTAPPLIED / INSPAYMENTAPPLIED to payment_reversed; that drops 70 reversed allocation rows ($39,990) into the reversed bucket on the dev DB and Collected lands at −$28,287 instead of the targeted +$11,538. Worker tightened the rule: `isReversed=true` flips ONLY cash codes (PATIENTPAYMENTS / INSURANCEPAYMENTS / refund codes) to `payment_reversed`; reversed allocation legs stay at `payment_applied` because the paired PATIENTPAYMENTSDELETE row carries the cash reversal — double-classifying both would re-create the negative-Collected bug from a different angle. With this rule, the dev DB Collected lands at +$11,538.00, matching the DoD exactly. Surface to human partner if the literal interpretation was intended.
- 2026-05-30 | Worker | Migration step 2 (DELETE wrong-kind duplicates) added after step-3 UPDATE collided with the ENG-269 cross-pull UNIQUE on `(tenant_id, source_provider, source_kind, source_external_id, kind)` in the dev DB (42 legacy mislabeled rows had a correctly-classified sibling from a later pull). Step 2 drops the wrong-kind row when its correct sibling already exists, so the UPDATE in step 3 never has to fight the UNIQUE.
