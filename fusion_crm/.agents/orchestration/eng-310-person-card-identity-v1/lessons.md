# Lessons — ENG-310

- accountId is NOT a household key — it's the clinic's CareStack account
  (55,704 pids). Household = shared normalized phone/email. Nearly built
  on the dead key; the verification query caught it.
