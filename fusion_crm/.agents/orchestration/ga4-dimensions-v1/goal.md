# Goal — ga4-dimensions-v1 (ENG-478)

Extend GA4 ingest beyond the property-day rollup: capture channel (organic/paid/direct), per-site (hostName), top pages (landingPage/pagePath), and engagement metrics (engagedSessions/engagementRate/avgSessionDuration/bounceRate). Surface organic-vs-paid + top pages + engagement on the /analytics/seo GA4 tab. Read-only :runReport only; full-fidelity raw capture; no fake zeros. Base = main (ENG-468 merged).
