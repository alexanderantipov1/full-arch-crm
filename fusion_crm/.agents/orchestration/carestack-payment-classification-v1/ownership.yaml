mission: carestack-payment-classification-v1
linear_anchor: ENG-283
owner_role: orchestrator
owner_agent: claude-code
depends_on: ENG-257/267/269/270/271 (payment events, idempotency, location, payments page)
tasks:
  - task_id: ENG-283
    title: "Classify CareStack payments by transactionCode — fix Collected"
    linear_id: ENG-283
    linear_url: https://linear.app/fusion-dental-implants/issue/ENG-283/classify-carestack-payments-by-transactioncode-fix-collected-double
    worker_role: worker
    worker_agent: claude-code
    workspace: worktree
    owns:
      - packages/db/alembic/versions/   # new revision: payment_applied kind + reclassify backfill
      - packages/interaction/models.py
      - packages/interaction/schemas.py
      - packages/interaction/service.py
      - packages/interaction/repository.py
      - packages/interaction/CLAUDE.md
      - packages/ingest/carestack_accounting_transaction_service.py
      - apps/api/routers/dashboard.py
      - apps/web (payments page toggle + dashboard collected)
      - tests/
notes:
  - "Classify by transactionCode, not folioType. New kind payment_applied (excluded from Collected)."
  - "Migration adds the kind + reclassifies existing events by raw transactionCode (append-only exception)."
  - "Read-only CareStack. No PHI."
