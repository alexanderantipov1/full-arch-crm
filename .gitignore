node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# HIPAA audit logs — append-only, gitignored but directory is tracked
server/audit/logs/*.log

# Communication queue — append-only NDJSON, gitignored but directory tracked
server/communication/queue.json